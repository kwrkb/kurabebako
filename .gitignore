# Hugo
/public/
/resources/_gen/
/.hugo_build.lock
/.cache/
hugo_stats.json

# Hugo Modules（vendor する場合のみコミットする）
/_vendor/

# Node / wrangler
/node_modules/
/.wrangler/

# OS / editor
.DS_Store
*.swp

# 型検査の回帰テスト（tests/post-checks.js）が一時的に置く記事。強制終了で残っても混ざらないように
/content/posts/zz-fixture-*.md
