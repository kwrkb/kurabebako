# LESSONS

判断が起きたその場で1エントリ追記する。**追記専用**（過去エントリは書き換えない）。
各エントリは `却下した案` / `決め手`（観測事実のみ）/ `覆す条件` の3項目。

---

## ビルドツールの版をホスティング側の環境変数で固定する

**却下した案**
ダッシュボードのビルド変数にサイトジェネレータのバージョンを設定し、版を固定する。

**決め手**
ビルドイメージには拡張版がプリインストールされているが、この環境変数で版を上書きすると
標準版がインストールされるという報告がある。標準版には Sass トランスパイラと
WebP エンコーダが含まれない。ビルドスクリプト内で拡張版の tarball を明示的に取得すれば、
ローカルと CI で同一バイナリになり、設定がダッシュボードとリポジトリに分散しない。

**覆す条件**
プラットフォーム側が拡張版の指定に対応した場合。または Sass / WebP を一切使わないと確定した場合。

---

## ダッシュボードのビルドコマンド欄にビルドコマンドを書く

**却下した案**
ホスティング側のダッシュボードにある Build command 欄に `npm run build` を入れる。

**決め手**
Deploy command が実行するデプロイツールは、設定ファイルに書かれたビルドコマンドを
自動的に先に走らせる。両方に書くとビルドが2回走り、しかも1回目はイメージ同梱の
古い版のジェネレータで実行される（最終出力は2回目に上書きされるため結果は正しくなるが、
ログからどちらの版でビルドされたか読めなくなる）。欄を空にしたビルドログで、
カスタムビルドが1回だけ走ることを確認した。

**覆す条件**
設定ファイル側のビルド定義を廃止してダッシュボードに一本化する場合。
その際はローカルの実行スクリプトもビルドを明示的に呼ぶ必要がある。

---

## モジュール初期化時に書かれた言語バージョンをそのまま残す

**却下した案**
モジュール初期化コマンドがローカルの処理系バージョンを元に書いた宣言を、そのまま残す。

**決め手**
ビルド環境の処理系はそれより古く、既定のツールチェーン設定では
宣言されたバージョンを満たすツールチェーンをビルド中にダウンロードしに行く。
遅くなるだけでなく、取得が塞がれていれば失敗する。宣言をビルド環境の版以下に下げると
取得自体が発生しない。実際のビルドログで、下げた宣言のまま依存解決が完了することを確認した。

**覆す条件**
ビルド環境の処理系が上がった場合。または新しいバージョン固有の機能が必要になった場合。
なお依存更新コマンドがこの宣言をローカルの版に書き戻すことがあるため、更新後は毎回確認する。

---

## 記事の日付を「これから公開する時刻」で入れる

**却下した案**
記事の front matter の日付を、公開予定時刻（現在時刻より後）で書く。

**決め手**
現在時刻より後の日付を持つ記事は、警告もエラーも出ないまま出力から除外された。
ビルドログのページ数が減ることでしか気づけず、原因の特定に時間がかかる。
日付を過去にした途端、ページ数が 11 から 20 に増えて記事とタクソノミが生成された。

**覆す条件**
予約投稿を運用に組み込む場合。その際は未来分を含めるビルドオプションを使い、
必ずプレビューで表示を確認する。

---

## デプロイツールを都度取得に任せる

**却下した案**
パッケージ定義を置かず、デプロイコマンドの `npx` に毎回最新版を取得させる。

**決め手**
ビルド環境はパッケージ定義があればクリーンインストールを自動実行する。
ピン留めしておけばその版がそのまま使われ、ローカルと CI が一致する。
都度取得だと、将来のメジャー更新が予告なくデプロイ経路に入る。
追加されるのは開発依存1つだけで、サイト自体の依存は増えない。

**覆す条件**
依存を完全にゼロにする方針に変える場合。

---

## 削除した記事が本番から消えない

**却下した案**
ビルドコマンドをそのまま走らせれば、削除した記事は次のデプロイで本番から消えると考える。

**決め手**
ホスティング側はビルドを速くするため、前回のビルド出力をキャッシュから復元してから
ビルドを走らせる。サイトジェネレータは自分が生成しないファイルを削除しないので、
消したはずの記事の HTML が出力ディレクトリに残り、そのまま再アップロードされる。
実際に、記事を削除して再デプロイした後もその URL が 200 を返し続けた
（sitemap からは消えていたため、新しいビルド自体は走っていた）。
ビルドの前に出力ディレクトリを削除すると解消する。

ジェネレータ側にも出力先を掃除するフラグはあるが、ヘルプの説明と実際の挙動が
一致していなかったため採用しなかった。明示的に消すほうが意図が読める。

**覆す条件**
ホスティング側が削除ファイルを検知してアセットから外すようになった場合。
なお依存キャッシュ（モジュールのダウンロード）は消していないので、ビルド時間への影響はない。

---

## URLパターンの置換変数をドキュメントの例どおりに番号付けする

**却下した案**
リダイレクト設定で、パスに対応する置換変数を公式ドキュメントの例に合わせて1番目と書く。

**決め手**
パターンがスキーム部分にもワイルドカードを含む場合（平文と暗号化の両方を1つのパターンで
拾う書き方）、置換番号はパターン内のワイルドカードを左から順に数える。
実測では、暗号化アクセス時に1番目の変数が `s`、平文アクセス時に空文字になった。
スキームのワイルドカードが1番目を占めており、パスは2番目だった。
公式ドキュメントの例はこれと異なる番号付けで書かれている。

リダイレクト自体は成功ステータスを返すため、飛び先を実際に追跡するまで壊れていると
気づけない。設定直後に、ルート・パス付き・クエリ付き・平文の4パターンで
最終到達先まで確認する。

**覆す条件**
プラットフォーム側が番号付けを変更した場合。
なお、ホスト名で判定してリクエストURIを連結する式で飛び先を組めば、
番号を数える必要自体がなくなるため、この問題は起きない。

---

## PR 表記をレイアウト側で自動挿入する

**却下した案**
本文にアフィリエイトリンクがあればテンプレートが本文冒頭に PR 表記を差し込む。書き手は何もしなくてよい。

**決め手**
テーマの単一ページテンプレートは、見出し・メタ情報の後で本文を描画する。本文より前の位置に
差し込むにはテンプレート全体を複製して上書きするしかなく、テーマ更新のたびに差分を追う必要が出る。
一方、shortcode は本文中の出現順に処理されることが確認できた（表記用 shortcode とリンク用 shortcode の
順序を入れ替えると、リンク側で「表記が未宣言」と検出できた）。
これにより「表記は書き手が置く。ただし順序と有無はビルドが検査する」構成にでき、
テンプレートの複製なしで、表記がリンクより先に読者の目に入ることを保証できる。

**覆す条件**
テーマが本文より前に呼ばれる拡張用 partial を提供した場合。

---

## 案件 id の未登録を警告にとどめる

**却下した案**
台帳に無い案件 id を参照した記事は、警告を出してリンク先を空にしたまま公開する。

**決め手**
ホスティング側のビルドログは自分から見に行かないと読まない。実際、テーマ由来の非推奨警告が
毎回2件出ているが、ビルドは成功扱いなので誰も止まらない。案件終了で台帳から消したとき、
記事側の直し忘れは警告では本番に出てしまう。ビルドエラーにすればデプロイ自体が止まり、
直すまで古い本番が残る。テンプレートの `errorf` はその場で実行を止めないことも観測したので、
未登録時は仮の値を返して後続の nil 参照を防ぎ、本命のエラーメッセージが埋もれないようにした。

**覆す条件**
記事数が増えて、1件の案件終了で複数記事のデプロイが長時間止まることが常態化した場合。
そのときは未登録 id のリンクだけを非表示にし、一覧を警告で出す方式に切り替える。

---

## 検証区分「実行」を増やすために検証用のアカウントやリポジトリを作る

**却下した案**
比較対象ごとに無料アカウントを作り、検証用リポジトリを公開して実デプロイし、全対象を「実行」区分にする。

**決め手**
アカウント作成はメール認証と利用規約への同意を伴い、AI が単独で行うと運営者名義のアカウントを
本人の承認なしに増やすことになる。Git ホスティングは既存の認証があったが、公開リポジトリの新規作成は
外から見える資産を増やす操作で、記事 1 本の裏取りのために事前承認なしに行う理由が薄い。
結果として 5 対象中 4 対象が「仕様」区分になったが、結論で推す対象はサイト自身が稼働している構成なので、
「主役は実行区分」の要件は満たせた。実行区分の裏取りは、稼働中の本番に対する `curl` と
ローカルビルドのファイル数という、副作用のない操作だけで済んだ。

**覆す条件**
運営者が検証専用のアカウントや組織を用意し、そこでの作成・削除を包括的に許可した場合。
または、主役にしたい対象がサイト自身の構成と異なり、公式ドキュメントだけでは「実行」を名乗れなくなった場合。

---

## 検証用 VPS の認証情報をファイルで渡す

**却下した案**
Terraform の `tfvars` や `.env` に API ユーザーのパスワードを書き、gitignore で守る。
もう一つ、SSH 鍵は provider に生成させる。

**決め手**
1Password のサービスアカウントと `op run` を使うと、参照文字列（`op://...`）だけをファイルに置き、
値は子プロセスの環境変数にしか現れない。実際に apply から destroy まで通したが、
AI 側の出力に値は一度も出なかった（長さの確認だけで動作検証できた）。
参照文字列は秘密ではないので commit でき、desk の lab を再現可能な形で残せる。
provider 生成の鍵は `private_key` が tfstate に平文で入ると provider のスキーマに明記されている。
ローカル生成して公開鍵だけ渡せば tfstate に秘密が入らない。

**覆す条件**
1Password のサービスアカウントが使えなくなった場合。
その場合でも、tfvars ではなく OS の資格情報ストアから環境変数に注入する方式を先に検討する。

---

## 作成直後の VPS に対して即座に apt を使う

**却下した案**
インスタンス作成後、SSH が通った時点で計測用パッケージを `apt-get install` する。

**決め手**
ConoHa の Ubuntu 24.04 テンプレートは初回起動直後に unattended-upgrades が走り、
237 パッケージ（カーネル含む）の更新で dpkg のロックを 10 分以上握った。
`-qq` で出力を抑えていたため、失敗が「install took 2s」としか見えず、原因特定に 2 回分の実行を使った。
ロック解放を `fuser` で待ってから apt を呼ぶと通った。
なお `speed.cloudflare.com/__down` は VPS からのアクセスに 403 を返し、帯域計測に使えなかった。

**覆す条件**
テンプレートの更新頻度が上がり、初回起動時の更新が数秒で終わるようになった場合。
それでもロック待ちのガードは残す（コストがゼロなので）。

## ネタ探しの定期実行に CronCreate を使う

**却下した案**
セッション内の CronCreate で毎日 1 回のジョブを立て、ネタ探しを回す。

**決め手**
CronCreate のジョブはセッション内のメモリにだけ載り、ディスクに書かれない。
Claude を終了した時点で消え、残ったとしても再帰ジョブは 7 日で自動失効する。
1 日 1 回を年単位で続ける前提と合わない。
scheduled-tasks 側は `~/.claude/scheduled-tasks/<id>/SKILL.md` に書き出され、
アプリを閉じている間に発火時刻を過ぎても次回起動時に実行される。

**覆す条件**
CronCreate に永続化オプションが入り、7 日の自動失効が外れた場合。
ただしそのときも、実行ごとに会話の記憶が無い前提でプロンプトを自己完結させる要件は変わらない。

## ネタ探しのレンズを曜日で切り替える

**却下した案**
1 回の実行で「発掘・昇格・鮮度点検・収益逆引き」を全部回す。

**決め手**
毎日 4 種を回すと同じ鉱脈を掘り返して重複候補が出るうえ、1 回が長くなって続かない。
初回の実行（2026-09-03、収益逆引き）は 1 レンズだけで公式ページ 6 件の確認に収まり、
候補 2 件と空振り 2 件を出せた。
また、既存の候補は 9 件あって不足していない。詰まっていたのは「どれを書くか決まらない」ことで、
そこには発掘ではなく点数付けと昇格判定が要る。だから昇格に週 2 日を割いた。

**覆す条件**
`候補` が 5 件を切って発掘が追いつかなくなった場合。
その場合も 1 日 1 レンズは崩さず、発掘レンズの日数を増やして対応する。

## Uptime Kuma の実行検証をローカルの Node.js で行い、VPS に立てなかった

**却下した案**
死活監視の記事で Uptime Kuma を「実行」区分にするために、`lab/conoha/` の Terraform で ConoHa VPS を 1 時間だけ立て、
Docker で動かす。VPS 記事との導線にもなる。

**決め手**
ローカルは `docker: command not found` だが Node.js 24.18.0（fnm）があり、Uptime Kuma 2.5.3 の要件（Node 20.4 以上）を満たす。
`npm run setup` 39 秒、起動から HTTP 応答まで 133 秒、内部 API で管理者作成〜モニター追加〜初回チェック（0.2 秒）〜削除まで
費用ゼロで通り、記事に必要な「AI が動かして確認した」事実はすべて取れた。
VPS で得られる追加情報は「Docker で動く」ことだけで、それは公式 wiki に書いてある。

**覆す条件**
記事の比較軸に「サーバー上での常時稼働時のメモリ・CPU」や「ステータスページの公開」が入った場合。
その時は VPS 記事の常設サーバー（段階 2）に立てて実測する。

## 未来日付で消えた記事を、記事ページの検査で捕まえる

**却下した案**
`extend_post_content.html`（記事ページの描画後に走る型の検査）に「date が現在より後なら止める」を足す。
もう一つ、`hugo server` を壊さないよう `hugo.IsServer` で検査を切り替える。

**決め手**
未来日付の記事は Hugo がページを生成しないので、記事ページの検査はそもそも呼ばれない。
`draft = false` で date を 2099 年にした記事を置いて `hugo -D` を実行したところ、記事ページ側の検査は 1 行も出ず、
トップページの描画時に `content/posts/` のファイル一覧と `site.RegularPages` を突き合わせる検査だけが
「draft ではないのにビルド対象に入っていません」で止まった（他の 2 つの検査と同時に 3 件のエラーとして出た）。
`npm run dev` は `--buildFuture` 付きなので未来日付でもページが生成され、この突き合わせは自然に通る。
`hugo.IsServer` での切り替えは要らなかった。

**覆す条件**
Hugo が未来日付・expiryDate による除外を警告として出すようになった場合。
または予約投稿を運用に組み込み、push 無しで再ビルドが走る仕組み（cron トリガー等）を入れた場合。

## OGP 画像を画像生成ツールに頼らず、図形描画のスクリプトで作った

**却下した案**
desk の `images/gen.sh`（agy の generate_image を呼ぶ）で 4 本目の OGP 画像を生成する。
止まった際に、ツールが案内する `--dangerously-skip-permissions` を付けて再実行する。

**決め手**
Claude Code から呼んだ agy は `escalate_admin` の権限要求をヘッドレスでは確認できず自動拒否し、
「no output produced」で画像を出さなかった（過去 3 本は対話セッションで承認して通っていた）。
案内された回避フラグは全ツールを自動承認するもので、画像生成 1 回のために AI が権限設定を緩める理由が無い。
同じ仕様（1200x630、紺の背景、ティールとアンバーの図形、右下のワードマークのみ）は Pillow の図形描画で再現でき、
`uv run --with pillow` で外部依存なしに 33 KB の JPEG が出た。生成モデルを使わないぶん、再生成が決定的になる。

**覆す条件**
運営者が agy 側の設定に `escalate_admin` の allow ルールを入れた場合。
または、図形だけでは表せないモチーフ（写実的な質感など）が必要になった場合。
