# 改行はリポジトリ全体で LF に固定する。
# layouts/ の partial と shortcode（bars / ranking）は本文を行単位で正規表現に掛けるため、
# core.autocrlf=true の Windows で CRLF に変換されると行末の \r が「449\r」のように数値や
# 行末一致（— YYYY-MM-DD 確認$）に混ざり、ビルドが止まる。
# build.sh も同じ理由（bash が \r を構文エラーにする）で LF が必要。
* text=auto eol=lf
*.sh text eol=lf
